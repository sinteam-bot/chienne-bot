/**
 * src/modules/util_embed_builder/config/defaults.js
 *
 * Configuration par défaut pour le Message Embedder persistant (Phase 12 G40).
 */

module.exports = {
    enabled: true,
    max_embeds_per_guild: 100
};
