/**
 * defaults.js — Commandes d'info (Phase 8.3)
 *
 * - enabled : master toggle
 * - color / footer : branding des embeds d'info
 * - show_id : afficher les IDs Discord dans les embeds (utile pour les devs)
 */

module.exports = {
    enabled: true,
    color: '#5865F2',
    footer: 'Bot — Information',
    show_id: true
};
