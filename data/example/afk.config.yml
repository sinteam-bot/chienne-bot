# data/example/afk.config.yml
#
# Template de configuration pour la feature "afk".
# Généré automatiquement depuis src/modules/feature_afk/config/defaults.js.
#
# Ne PAS modifier manuellement ici : c'est un exemple versionné.
# Pour modifier la config par défaut, édite plutôt :
#   data/default/afk.config.yml  (modifiable par l'admin, gitignore)
#   data/{guildId}/afk.config.yml  (override par guilde, gitignore).
enabled: true
auto_remove_on_message: true
notify_on_mention: true
delete_notification_after_seconds: 10
