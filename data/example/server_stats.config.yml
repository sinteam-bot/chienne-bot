# data/example/server_stats.config.yml
#
# Template de configuration pour la feature "server_stats".
# Généré automatiquement depuis src/modules/feature_server-stats/config/defaults.js.
#
# Ne PAS modifier manuellement ici : c'est un exemple versionné.
# Pour modifier la config par défaut, édite plutôt :
#   data/default/server_stats.config.yml  (modifiable par l'admin, gitignore)
#   data/{guildId}/server_stats.config.yml  (override par guilde, gitignore).
enabled: true
update_interval_minutes: 10
allowed_types:
  - total_members
  - human_members
  - bot_members
  - channel_count
  - role_count
