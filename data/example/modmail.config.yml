# data/example/modmail.config.yml
#
# Template de configuration pour la feature "modmail".
# Généré automatiquement depuis src/modules/feature_modmail/config/defaults.js.
#
# Ne PAS modifier manuellement ici : c'est un exemple versionné.
# Pour modifier la config par défaut, édite plutôt :
#   data/default/modmail.config.yml  (modifiable par l'admin, gitignore)
#   data/{guildId}/modmail.config.yml  (override par guilde, gitignore).
enabled: true
guild_id: null
category_id: null
log_channel_id: null
staff_roles: []
anonymous_replies_by_default: false
greeting_message: 👋 Bonjour ! Votre message a bien été reçu par l’équipe de
  modération. Un modérateur vous répondra dès que possible.
closing_message: 🔒 Ce fil d’assistance ModMail a été clôturé. N’hésitez pas à
  renvoyer un message si vous avez une autre question.
