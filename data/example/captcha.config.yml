# data/example/captcha.config.yml
#
# Template de configuration pour la feature "captcha".
#
# Ne PAS modifier manuellement ici : c'est un exemple versionné.
# Pour modifier la config par défaut, édite plutôt :
#   data/default/captcha.config.yml  (modifiable par l'admin, gitignore)
#   data/{guildId}/captcha.config.yml  (override par guilde, gitignore).

enabled: true
log_channel_id: null
verified_role_id: "1337917252732850206"
captcha_channel_name: "✅-verification-captcha"
captcha_timeout: 10
max_attempts: 3
# Type de captcha proposé aux nouveaux membres :
#   - math   : calcul arithmétique en texte (défaut, comportement actuel)
#   - image  : reconnaissance de texte sur une image (PNG générée par canvas)
#   - web    : page HTML externe avec hCaptcha (variable HCAPTCHA_SITE_KEY requise)
#   - audio  : TTS audio (math vocal) — implémenté en phase 2
captcha_type: math
use_word_operators: false
math_questions:
  min_number: 1
  max_number: 20
  use_word_operators: false
  word_operators:
    "+": "plus"
    "-": "moins"
    "*": "fois"
  operations:
    - "+"
    - "-"
    - "*"
  operation_weights:
    "+": 0.6
    "-": 0.3
    "*": 0.1
messages:
  welcome_message: "Bienvenue sur le serveur ! Pour accéder au reste du serveur, tu dois résoudre ce captcha :"
  captcha_question: "Combien font {num1} {operator} {num2} ?"
  instructions: "Réponds avec le résultat **en chiffres** (exemple: 18) dans ce canal."
  success_message: "✅ Bravo ! Ta réponse est correcte. Tu as maintenant accès au serveur !"
  fail_message: "Mauvaise réponse, tu n'as plus que {attempts} tentative(s)."
  timeout_message: "⏰ Temps écoulé ! Le captcha a expiré. Veuillez réessayer en rejoignant à nouveau le serveur."
  max_attempts_message: "❌ Trop de tentatives incorrectes. Veuillez quitter et rejoindre le serveur pour réessayer."
  already_verified: "✅ Tu as déjà été vérifié !"
