# data/example/autothread.config.yml
#
# Template de configuration pour la feature "autothread".
# Généré automatiquement depuis src/modules/feature_autothread/config/defaults.js.
#
# Ne PAS modifier manuellement ici : c'est un exemple versionné.
# Pour modifier la config par défaut, édite plutôt :
#   data/default/autothread.config.yml  (modifiable par l'admin, gitignore)
#   data/{guildId}/autothread.config.yml  (override par guilde, gitignore).
enabled: true
channels: []
